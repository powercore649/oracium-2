<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="AppGroup.BackupImportDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    CornerRadius="5"
    xmlns:local="using:AppGroup">

    <ContentDialog.Resources>
        <Thickness x:Key="ContentDialogPadding">0,0,0,0</Thickness>
    </ContentDialog.Resources>

    <StackPanel Spacing="10" VerticalAlignment="Center" MinWidth="400"
                Background="{ThemeResource LayerOnMicaBaseAltFillColorSecondaryBrush}"
                CornerRadius="5" HorizontalAlignment="Stretch">

        <!-- Title bar -->
        <Grid Padding="10,0,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto" MinWidth="100"/>
            </Grid.ColumnDefinitions>
            <TextBlock FontSize="20" Margin="10" VerticalAlignment="Center"
                       FontWeight="SemiBold">Import from Backup</TextBlock>
            <Button x:Name="CloseButton" Grid.Column="1" Height="35"
                    HorizontalAlignment="Right" Background="Transparent" BorderThickness="0">
                <FontIcon Glyph="&#xe711;" FontSize="15"/>
            </Button>
        </Grid>

        <!-- Content -->
        <StackPanel Spacing="8">

            <ScrollViewer MaxHeight="400" Padding="20,10,20,10" VerticalScrollBarVisibility="Auto">
                <ListView x:Name="GroupListView"
                          MaxHeight="380"
                          Padding="-15,0,-15,0"
                          SelectionMode="None"
                          CornerRadius="5">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,1,0,0" HorizontalAlignment="Stretch" CornerRadius="5"
                                  Padding="10" BorderThickness="1"
                                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                  Background="{ThemeResource CardBackgroundFillColorDefault}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="35"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <CheckBox Grid.Column="0" Width="35"
                                          IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                          VerticalAlignment="Center" Margin="0,0,8,0"/>

                                <!-- Group icon -->
                                <StackPanel Grid.Column="1" Spacing="5" VerticalAlignment="Center"
                                            Padding="6" Orientation="Vertical" BorderThickness="1"
                                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                            Background="{ThemeResource CardBackgroundFillColorDefault}"
                                            CornerRadius="5" Margin="0,0,12,0">
                                    <Image Source="{Binding GroupIcon}" Width="35" Height="35" Stretch="UniformToFill"/>
                                </StackPanel>

                                <!-- Group info -->
                                <!-- Group info -->
                                <StackPanel Grid.Column="2" Spacing="2" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding GroupName}" FontSize="15" FontWeight="SemiBold"/>
                                    <StackPanel Orientation="Horizontal">
                                        <ItemsControl ItemsSource="{Binding PathIcons}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel BorderThickness="1"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                Background="{ThemeResource CardBackgroundFillColorDefault}"
                                Margin="1" CornerRadius="2" Padding="3">
                                                        <Image Source="{Binding}" Width="15" Height="15"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                        </ItemsControl>
                                        <TextBlock Text="{Binding AdditionalIconsText}" FontSize="11"
                   VerticalAlignment="Center" Margin="5,0,0,5"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollViewer>

            <Grid Padding="25,0,25,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <CheckBox x:Name="SelectAllCheckBox"
                          Content="Select All"
                          Margin="12,0,0,0"
                          Padding="15,5"
                          IsChecked="True"
                          Checked="SelectAllCheckBox_Checked"
                          Unchecked="SelectAllCheckBox_Unchecked"/>

                <StackPanel Grid.Column="1" HorizontalAlignment="Stretch">
                    <Button x:Name="ImportButton"
                            HorizontalContentAlignment="Center"
                            HorizontalAlignment="Right"
                            Style="{StaticResource AccentButtonStyle}">
                        <TextBlock FontSize="14" FontWeight="SemiBold">Import</TextBlock>
                    </Button>
                </StackPanel>
            </Grid>
        </StackPanel>

    </StackPanel>
</ContentDialog>
