<Window
    xmlns:winuiex="using:WinUIEx"
    x:Class="AppGroup.PopupWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppGroup"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="App Group">
    <Window.SystemBackdrop>
        <winuiex:TransparentTintBackdrop />
        <!--<MicaBackdrop />-->
    </Window.SystemBackdrop>

    <!--SINGEL TONE TOP HEADER (DEFAULT)--> 

    <Grid x:Name="MainGrid"    >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>



        <ScrollViewer
            Grid.Row="1" VerticalScrollMode="Auto"
            CornerRadius="0" 
            Margin="30"
            x:Name="ScrollView"  
            VerticalContentAlignment="Center"
            VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
            HorizontalScrollMode="Disabled"
              VerticalScrollBarVisibility="Auto" >
            <StackPanel x:Name="GridPanel" Width="Auto"  Height="Auto"   VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </ScrollViewer>
        <StackPanel  Grid.Row="0" >
            <Grid x:Name="Header"  Visibility="Collapsed" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="HeaderText" VerticalAlignment="Center" FontSize="12" />
                <Button x:Name="HeaderEditButton" Grid.Column="1" Padding="7" VerticalAlignment="Center" Click="EditGroup_Click" BorderThickness="0" Background="Transparent">
                    <FontIcon Glyph="&#xe70f;" FontSize="13"/>
                </Button>

            </Grid>
        </StackPanel>
    </Grid>
    <!--TWO TONE TOP HEADER-->
    <!--<Grid x:Name="MainGrid"  Margin="0,0,0,-5"  >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>



        <ScrollView  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"  Background="{ThemeResource CardBackgroundFillColorDefault}"
                     Grid.Row="1" VerticalScrollMode="Auto" CornerRadius="0"  x:Name="ScrollView"  VerticalAlignment="Stretch" HorizontalAlignment="Stretch" HorizontalScrollMode="Disabled"
                  VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="GridPanel" Width="Auto" Height="Auto" Margin="0,0,-5,-15"  VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </ScrollView>
        <StackPanel  Grid.Row="0" >
            <Grid x:Name="Header" Margin="10,5,5,5" Padding="0" Visibility="Collapsed" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="HeaderText" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <Button Grid.Column="1" Padding="7" VerticalAlignment="Center" Click="EditGroup_Click" BorderThickness="0" Background="Transparent">
                    <FontIcon Glyph="&#xe70f;" FontSize="13"/>
                </Button>

            </Grid>
        </StackPanel>
    </Grid>-->
    <!--TWO TONE BOTTOM HEADER-->
    <!--<Grid x:Name="MainGrid"   >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>

            <RowDefinition Height="Auto"/>



        </Grid.RowDefinitions>



        <ScrollViewer x:Name="ScrollView" 
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
              Background="{ThemeResource CardBackgroundFillColorDefault}"
              Grid.Row="0"
              VerticalScrollMode="Auto"
              CornerRadius="0"
              VerticalAlignment="Stretch"
              HorizontalAlignment="Stretch"
              HorizontalScrollMode="Disabled"
              VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="GridPanel" Width="Auto" Height="Auto" Margin="0,0,-5,-15"
                VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </ScrollViewer>
        <StackPanel  Grid.Row="1" >
            <Grid x:Name="Header" Margin="10,0,5,5" Padding="0" Visibility="Collapsed" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="HeaderText" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <Button Grid.Column="1" Padding="7" VerticalAlignment="Center" Click="EditGroup_Click" BorderThickness="0" Background="Transparent">
                    <FontIcon Glyph="&#xe70f;" FontSize="13"/>
                </Button>

            </Grid>
        </StackPanel>
    </Grid>-->
    
    <!--SINGLE TONE BOTTOM HEADER-->
    <!--<Grid x:Name="MainGrid"  >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>

            <RowDefinition Height="Auto"/>



        </Grid.RowDefinitions>



        <ScrollView 
                     Grid.Row="0" VerticalScrollMode="Auto" CornerRadius="0"  x:Name="ScrollView"  VerticalAlignment="Stretch" HorizontalAlignment="Stretch" HorizontalScrollMode="Disabled"
                    VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="GridPanel" Width="Auto" Height="Auto"  Margin="0,0,-5,-20"  VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </ScrollView>
        <StackPanel  Grid.Row="1" >
            <Grid x:Name="Header" Margin="10,0,5,5"  Padding="0" Visibility="Collapsed" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="HeaderText" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <Button Grid.Column="1" Padding="7" VerticalAlignment="Center" Click="EditGroup_Click" BorderThickness="0" Background="Transparent">
                    <FontIcon Glyph="&#xe70f;" FontSize="13"/>
                </Button>

            </Grid>
        </StackPanel>
    </Grid>-->
</Window>
